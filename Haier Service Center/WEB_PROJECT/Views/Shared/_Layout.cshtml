<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Haier Service Center</title>

    <script>
        (function () {
            const theme = localStorage.getItem("theme") || "light";
            document.documentElement.setAttribute("data-bs-theme", theme);

            // set icon AFTER page loads
            window.addEventListener("DOMContentLoaded", () => {
                const btn = document.querySelector(".theme-toggle");
                if (btn) {
                    btn.innerText = theme === "dark" ? "☀️" : "🌙";
                }
            });
        })();
    </script>



    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <link href="~/lib/bootstrap-icons/font/bootstrap-icons.min.css" rel="stylesheet" />
    <link href="~/lib/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet" />

    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ServicePlatform.styles.css" asp-append-version="true" />


    @* <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.10.377/pdf.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.10.377/pdf_viewer.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/2.10.377/pdf.worker.min.js"></script>
 *@

    <script src="~/js/pdf.min.js"></script>
    <script src="~/js/pdf_viewer.min.js"></script>
    <script src="~/js/pdf.worker.min.js"></script>



    <style type="text/css">
        /* Style for the logo */
        .navbar-brand img {
            max-width: 100px; /* Set a maximum width for the logo */
            height: 30px; /* Maintain the logo's aspect ratio */
            margin-right: 0px; /* Add some spacing to the right of the logo */
        }

        /* Custom CSS for multi-level menu */
        .navbar .dropdown-submenu {
            position: relative;
        }

            .navbar .dropdown-submenu .dropdown-menu {
                left: 100%;
                top: 0;
                margin-top: 0;
            }


            .navbar .dropdown-submenu:hover > .dropdown-menu {
                display: block;
            }

    </style>

    <style type="text/css">
        /* Footer */
        .footer-flex {
            display: flex;
            justify-content: space-between; /* pushes left & right */
            align-items: center;
        }

        /* left items inline */
        .footer-left {
            display: flex;
            align-items: center;
            gap: 10px; /* spacing between text and link */
        }

    </style>


    <style>
        /* Style for webkit browsers (Chrome, Safari, Edge) */
        ::-webkit-scrollbar {
            width: 12px;
            background-color: #F5F5F5;
        }

        ::-webkit-scrollbar-track {
            -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
            border-radius: 10px;
            background-color: #F5F5F5;
        }

        ::-webkit-scrollbar-thumb {
            border-radius: 10px;
            -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,.3);
            background-color: #555;
        }

            ::-webkit-scrollbar-thumb:hover {
                background: #F5F5F5;
            }


        /* Style for Firefox */
        html {
            scrollbar-width: thin;
            scrollbar-color: #888 #f1f1f1;
        }

        /* .dropdown-toggle::after {
                                                            display: inline-block;
                                                            margin-left: .5em; /* Increased margin for better spacing */
        /* vertical-align: .2em; */ /* Adjusted vertical alignment */
        /* content: "" */;
        /* border-top: .4em solid; */ /* Increased size */
        /* border-right: .4em solid transparent;
                                                            border-bottom: 0;
                                                            border-left: .4em solid transparent; */
        /* transition: transform 0.3s ease; */ /* Smooth transition for rotation */
        /* }  */
        .dropdown-toggle::after {
            display: inline-block;
            margin-left: .255em;
            vertical-align: .255em;
            content: "";
            border-top: .3em solid;
            border-right: .3em solid transparent;
            border-bottom: 0;
            border-left: .3em solid transparent;
            transition: transform 0.1s ease;
        }

        /* Add a class to handle the active state when the menu is open */
        .dropdown-toggle.active::after {
            transform: rotate(180deg); /* Rotate the arrow to point to the up */
            /* margin-top: 0.1em; */ /* Adjust margin to vertically center after rotation */
        }

        .dropdown-item.active::after {
            transform: rotate(-90deg); /* Rotate the arrow to point to the left */
            /* margin-top: 0.1em; */
        }

    </style>



</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        @*         <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3 fixed-top">
 *@            
     <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-body-tertiary border-bottom box-shadow mb-3 fixed-top">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">
                    <img src="~/images/Dashboard Images/Baer Logo.png" alt="Company Logo" width="40" height="40">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">

                    @*                    <ul class="navbar-nav flex-grow-1">
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
					</li>
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area="" asp-controller="UserRoles" asp-action="Index">User Roles</a>
					</li>
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area="" asp-controller="RoleManager" asp-action="Index">Role Manager</a>
					</li>
					</ul>
					*@
                    @*     <ul class="navbar-nav flex-grow-1">
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
					</li>
					</ul>*@
                    @*    @if (User.Identity.IsAuthenticated)
					{
					@await Component.InvokeAsync("MyMenu")

					// 👇 new code
					<ul class="navbar-nav ms-auto">
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area=""
					asp-controller="Account"
					asp-action="Logout">Logout</a>
					</li>
					</ul>
					// 👆 new code
					}
					else
					{*@
                    @*                        <ul class="navbar-nav ms-auto">
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area=""
					asp-controller="Account"
					asp-action="Login">Login</a>
					</li>
					</ul>*@

                    <partial name="_LoginPartial" />


                    @*}*@

                    @*<ul class="navbar-nav flex-grow-1">
					<li class="nav-item">
					<a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
					</li>
					</ul>
					*@
                    @*@await Html.PartialAsync("_Navbar", Model)*@


                </div>
            </div>
        </nav>
    </header>
    <div class="container pt-3">
        <main role="main" class="flex-fill pt-5 container">
            @RenderBody()
        </main>
    </div>

    @*     <footer class="border-top footer text-muted py-0 small footer-compact">*@
    <footer class="footer mt-auto border-top p-sm-1 py-sm-2 small footer-compact">
        <div class="container d-flex justify-content-between align-items-center">
            <!-- LEFT SIDE -->
            <div class="d-flex gap-2">
                <span>&copy; Haier Service Center</span>
                <a asp-controller="Home" asp-action="Privacy">Privacy</a>
            </div>

            <!-- RIGHT SIDE -->
            <button onclick="toggleTheme()" class="btn btn-sm btn-outline-primary theme-toggle">
                🌙
            </button>

        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)


    <script>
        // Custom JavaScript to toggle submenus on click
        document.addEventListener("DOMContentLoaded", function () {
            const dropdownToggleList = Array.from(document.querySelectorAll(".dropdown-toggle"));
            dropdownToggleList.forEach(function (toggle) {
                toggle.addEventListener("click", function (e) {
                    e.preventDefault();
                    const submenu = this.nextElementSibling;
                    if (submenu.style.display === "block") {
                        submenu.style.display = "none";
                    } else {
                        submenu.style.display = "block";
                    }
                    e.stopPropagation(); // Prevent the click event from propagating to the document
                });
            });


            // Add a click event listener to the document to close submenus when clicking outside
            document.addEventListener("click", function (e) {
                const submenus = document.querySelectorAll(".dropdown-menu");
                submenus.forEach(function (submenu) {
                    if (!submenu.contains(e.target)) {
                        submenu.style.display = "none";
                    }
                });
            });


            //<!--Add this JavaScript to hide submenus initially-- >
            const submenus = document.querySelectorAll(".navbar .dropdown-menu");
            submenus.forEach(function (submenu) {
                submenu.style.display = "none";
            });

        });

        $(document).ready(function () {
            // Toggle dropdown active class on button click
            $('.dropdown-toggle').on('click', function () {
                $(this).toggleClass('active');
            });

            // Close dropdown and remove active class when clicking outside
            $(document).on('click', function (event) {
                if (!$(event.target).closest('.dropdown-toggle').length) {
                    $('.dropdown-toggle').removeClass('active');
                }
            });
        });

    </script>


    <script>
        function setThemeIcon(theme) {
            const btn = document.querySelector(".theme-toggle");
            if (!btn) return;

            btn.innerText = theme === "dark" ? "☀️" : "🌙";
        }

        function toggleTheme() {
            const html = document.documentElement;

            const current = html.getAttribute("data-bs-theme") || "light";
            const newTheme = current === "dark" ? "light" : "dark";

            html.setAttribute("data-bs-theme", newTheme);
            localStorage.setItem("theme", newTheme);

            setThemeIcon(newTheme); // 🔥 update icon
        }
    </script>




</body>
</html>




