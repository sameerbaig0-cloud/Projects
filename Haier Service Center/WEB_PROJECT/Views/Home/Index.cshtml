@{
    ViewData["Title"] = "Home Page";
}

@*<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>*@

<link href="~/css/dashboardanimation.css" rel="stylesheet" />
<link href="~/css/texttypeanimation.css" rel="stylesheet" />




<section>
    @*     <div class="content">
        <p>@ViewBag.CompanyName</p>
        <p>@ViewBag.CompanyName</p>
    </div>
 *@
    <div>
        <div class="content">
            <ul>
                <li>
                    <div id="typingText" class="responsive-text">@ViewBag.CompanyName</div>
                </li>
            </ul>
        </div>
    </div>

</section>



@* 
<script>
    document.addEventListener("DOMContentLoaded", function () {

        const text = `@ViewBag.CompanyName`;
        const container = document.getElementById("typingText");

        function animateText() {
            container.innerHTML = "";

            [...text].forEach((char, index) => {
                const span = document.createElement("span");

                span.innerHTML = char === ' ' ? '&nbsp;' : char;

                const directions = ["top", "left", "right", "bottom"];
                span.classList.add("letter", directions[index % 4]);

                span.style.animationDelay = (index * 0.05) + "s";

                container.appendChild(span);
            });
        }

        animateText();
    });
</script> *@



<script>
    const text = `@ViewBag.CompanyName`;
    const container = document.getElementById("typingText");

    function animateText() {
        container.innerHTML = "";

        [...text].forEach((char, index) => {
            const span = document.createElement("span");
            span.innerText = char;

            // Assign direction based on index
            const directions = ["top", "left", "right", "bottom"];
            span.classList.add("letter", directions[index % 4]);

            span.style.animationDelay = (index * 0.1) + "s";
            container.appendChild(span);
        });
    }

    // run first time
    animateText();

    // repeat every 10 seconds
    setInterval(animateText, 10000);
</script>

