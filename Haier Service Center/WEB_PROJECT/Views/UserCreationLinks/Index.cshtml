@model IEnumerable<ServicePlatform.Models.UserCreationLink>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ApplicationUser.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ApplicationUser.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
@*             <td>
                @Html.DisplayFor(modelItem => item.ActivationCode)
            </td>
 *@            <td>
                @Html.DisplayFor(modelItem => item.CreatedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
@*             <td>
                <a asp-action="Edit" asp-route-id="@Encrypt.Encrypt(item.CreationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@Encrypt.Encrypt(item.CreationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@Encrypt.Encrypt(item.CreationID">Delete</a>
            </td>
 *@        </tr>
}
    </tbody>
</table>
